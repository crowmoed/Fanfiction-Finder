services:
  backend:
    build: .
    container_name: ficfinder-backend
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DATABASE_URL=${DATABASE_URL}
    command: >
      sh -c "python db/postgres.py && uvicorn api:app --host 0.0.0.0 --port 8000"
